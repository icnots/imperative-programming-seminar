#include "my_utils.h"


int main(){
    int *(*function)() = min;

    int a = 3, b = 5;
    int *p = function(&a, &b);
    (*p)++;
    f(a, b);
    swap(&a, &b);
    p = function(&a, &b);
    (*p)++;
    f(a, b);
    
    func();
    
    func();
    
    func();
    return 0;
}

